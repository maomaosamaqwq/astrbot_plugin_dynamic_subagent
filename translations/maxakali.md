# Maxakalí (Kumanák) — Dynamic SubAgent

Dynamic SubAgent AstrBot plugin, agente sub-agent max. Max max (safe/medium/full) max, sub-agent max sub-agent max. Max max, max max, max max max.

---
*This introduction is written in Maxakalí (Maxakalí).*
