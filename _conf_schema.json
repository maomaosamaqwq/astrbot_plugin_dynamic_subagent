{
  "model_blacklist": {
    "type": "list",
    "description": "模型黑名单，AI 创建子 Agent 时不能选择这些模型",
    "default": []
  },
  "model_filter_mode": {
    "type": "string",
    "description": "模型过滤模式",
    "options": ["blacklist", "whitelist"],
    "labels": ["黑名单", "白名单"],
    "default": "blacklist"
  },
  "allowed_models": {
    "type": "list",
    "description": "模型白名单（仅 whitelist 模式生效）",
    "default": []
  },
  "max_handoffs_per_event": {
    "type": "int",
    "description": "单次对话中 handoff 转交次数上限",
    "default": 20
  },
  "max_spawns_per_event": {
    "type": "int",
    "description": "单次对话中最多可创建的子 Agent 数量",
    "default": 10
  },
  "max_context_turns": {
    "type": "int",
    "description": "每个 Agent 保留的最大对话历史轮数",
    "default": 20
  },
  "trace_enabled": {
    "type": "bool",
    "description": "是否启用子 Agent 协作追踪记录",
    "default": true
  }
}
